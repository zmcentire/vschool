import React, {Component} from 'react';
import HeroList from './heroList';

class HeroEvent extends Component {
    constructor(props) {
        super(props);
        this.state = {
            
        }
    }
}