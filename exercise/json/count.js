const arrCode = ["code", "code", "code", "cool"]


function countCode(arrCode) {
    let count = 0
    for (let i=0; i < arrCode.length; i++) {
        if(arrCode[i] === "code"){
            count += 1
        } 
    } return count
}

function codeCount(str) {
    let count = 0
    let index = str.indedOf("c")
    for(i=0; i < str.length; i++){
        if(str[+1] === "o" && str[+3] === "e")
    }
}